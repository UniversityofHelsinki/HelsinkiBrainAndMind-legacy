<?php

/**
 * @file
 */

/**
 * Implements hook_preprocess_html().
 *
 * {@inheritdoc}
 */
function hbm_preprocess_html(&$variables) {
  global $base_secure_url;

  $variables['theme_path'] = file_create_url($base_secure_url . '/' . drupal_get_path('theme', 'hbm'));
}
